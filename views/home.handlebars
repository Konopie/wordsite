
<div class="col-md-11">
    <h1 id="welcome">Welcome to WordSite {{username}}</h1>
    <p id="user-name"></p>
    <p>We let you post words.</p>
</div>

<div class="post-list">
  {{#each posts as |post|}}
  <div class="card">
    {{> post-info post}}
  </div>
  {{/each}}
</div>

